server:
  port: 8082

spring:
  application:
    name: fraud
  datasource:
    url: jdbc:mysql://localhost:3306/fraud
    username: user
    password: 1234
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true
